# 更新日志

## [1.5.1] - 2026-08-25

### 修复
- RawMessage 解析改用 `ast.literal_eval`（源日志为 Python 单引号 repr，非标准 JSON），修复 rawmessage 模式识别失败问题

## [1.5.0] - 2026-08-25

### 新增
- **RawMessage 日志源**：auto 检测优先级调整为 `group_chat_context` > `rawmessage` > `event_bus`。开启 DEBUG 但 GC 行因管线过滤不生成时，自动改用 RawMessage 行（含群号/昵称/内容），**无需调整 empty_mention_waiting 即可按群分文件**

## [1.4.8] - 2026-08-23

### 新增
- **群白名单** `group_whitelist`：仅归档白名单中的群（消息+图片），留空=全部群

## [1.4.7] - 2026-08-23

### 修复
- 导出时自动规范化 AstrBot 偶发的双平台前缀（`pre-config:pre-config:` → `pre-config:`）
- 修正脱敏替换串硬编码前缀导致的重复问题（不影响通用匹配符）

## [1.4.6] - 2026-08-23

### 新增
- **Windows 兼容**：旧脚本进程检测支持 Windows（PowerShell/wmic），不再依赖 Linux 的 pgrep

## [1.4.5] - 2026-08-23

### 新增
- **图片按群分目录**：`tu/<群号>/`（脱敏开启时为群号哈希），清理逻辑同步支持子目录

## [1.4.4] - 2026-08-23

### 新增
- 启动时检测旧版导出脚本 `export_group_logs.py` 是否在运行，若存在则警告（防止与插件共用状态文件导致日志重复导出）

## [1.4.3] - 2026-08-23

### 修复
- 兼容新版 AstrBot（4.27+）group_chat_context 格式：平台标识为账号名（如 `账号1:GroupMessage:群号`）时也能识别并分群，不再回退 unknown

## [1.4.2] - 2026-08-23

### 修复
- 版本号调整以适配 Cloud 市场发布（v1.4.1 版本号已被占用）

## [1.4.1] - 2026-08-23

### 修复
- README 补充 AI 命名隐私说明（响应市场安全审核建议）

## [1.4.0] - 2026-08-23

### 新增
- **event_bus 兼容模式**：未开启 DEBUG 的 AstrBot 也能记录聊天（`log_source` 配置，自动检测回退到 INFO 级 event_bus 日志）
- **auto_enable_debug**：检测到未开 DEBUG 时自动修改 AstrBot 配置（`log_level=DEBUG` + 文件日志），重启后生效
- **日志自动清理**：`log_cleanup` 配置（如 `3day/00:00:00:000`），按天数定时清理归档日志

### 优化
- 兼容无日期格式的日志行（如 `[19:35:02.269]`）

## [1.3.0] - 2026-08-22

### 新增
- **图片实时保存**：事件监听驱动，纯图片/文件/表情消息即时保存到 `tu/` 目录并写入归档记录（不依赖日志）
- **AI 图片命名**：`image_caption` 开关，调用识图模型生成 5-6 字名称（支持 xiaomi/mimo 推理模型直连，自动压缩提速，后台异步不阻塞）
- **图片自动清理**：`cleanup_images` 开关 + `image_retention_days`（默认7天）定时清理过期图片
- 定时支持简单时间格式 `HH:MM`（如 `12:00`）

### 优化
- AI 识别前压缩图片，速度提升 10 倍+（约 3 秒），保存的仍为原图

## [1.2.0] - 2026-08-22

### 新增
- **cron 定时**：支持 cron 表达式自定义导出时间（如 `0 3 * * *` 每天凌晨3点、`*/5 * * * *` 每5分钟），留空则使用间隔模式

## [1.1.0] - 2026-08-22

### 新增
- **分群归档**：每个群独立归档文件 `astrbot_<群号>_YYYY-MM-DD.log`，不再所有群混在一起
- **脱敏配置**（默认关闭，可在插件配置中开启）：
  - `mask_group_id`：群号哈希脱敏（文件名与内容一致）
  - `mask_qq_id`：QQ 号打码（如 `343****565`）

### 修复
- README 图片改用标准 Markdown 语法，兼容各渲染器

## [1.0.2] - 2026-08-22

### 修复
- README 增加「文件保存在哪里」说明，明确归档路径
- logo 使用 GitHub 绝对链接，避免 Cloud 构建资源 404

## [1.0.1] - 2026-08-22

### 修复
- README 图片渲染兼容性
- 支持平台声明调整为 aiocqhttp

## [1.0.0] - 2026-08-22

### 新增
- 定时增量导出 AstrBot 群聊记录
- 只保留聊天记录行，过滤系统/LLM 日志（体积缩小 99%+）
- 按天归档，导出后清空源日志，避免双份增长
- 增量断点续传，AstrBot 重启/日志轮转不丢不重
- 插件卸载时最终导出
- 指令：`/log_archive status|now|clean|help`
